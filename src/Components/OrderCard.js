import React from 'react'

function OrderCard({order}) {
    order.cart.map(e=>{
        console.log(e);
    })
    return (
        <div>
            
        </div>
    )
}

export default OrderCard
